# Jarvis-core-v1
This AI core merges responses from two different AIs using their APIs, model names, and websites to deliver better answers. Just follow the code to see how it works. If no README is shown, add this to the command first:pip install requests edge-tts playsound psutil pygetwindow
